# glovewort phase-1 OpenClaw config skeleton
# Baseline: OpenClaw 2026.4.2
# Spec-first only; not yet validated for runtime.

models:
  defaults:
    model:
      primary: "ollama-bridge/qwen2.5-coder:1.5b"
  providers:
    ollama-bridge:
      api: "openai-completions"
      baseUrl: "http://llm-bridge-api:8000/v1"
      apiKey: "${BRIDGE_API_KEY}"
      models:
        "qwen2.5-coder:1.5b": {}

# Telegram provider wiring and runtime-specific settings
# will be finalized after config contract review.
